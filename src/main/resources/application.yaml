spring:
  application:
    name: MobilePaluwagan
  profiles:
    active:
      - dev


---
spring:
  config:
    activate:
      on-profile: dev

  datasource:
    url: jdbc:postgresql://aws-1-ap-southeast-1.pooler.supabase.com:6543/postgres
    username: postgres.bialnvehadzzryfbzwol
    password: Supabase123!
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
